<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dashboard">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dashboard">
        <activity
            android:name=".ui.activities.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true">
        </activity>
        <activity android:name=".ui.activities.SecondActivity"></activity>
        <activity android:name=".ui.activities.BMI"></activity>
        <activity android:name=".ui.activities.FoodDiary"></activity>
        <activity android:name=".ui.activities.RewardPage"></activity>
        <activity android:name=".ui.introscreen.IntroActivity"></activity>

    </application>

</manifest>